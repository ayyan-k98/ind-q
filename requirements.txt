# Core dependencies for coverage environment
numpy>=1.21.0
networkx>=2.8.0
torch>=2.0.0
pyyaml>=6.0

# Note: EPyMARL installation handles most dependencies
# Install EPyMARL first: pip install -e . (from epymarl directory)
